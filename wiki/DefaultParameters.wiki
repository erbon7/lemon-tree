#summary All the default parameters.

= ganesh task =


||Parameter|| Default value || Description ||
||lambda || 0.1 || lambda parameter of the bayesian inference model||
||alpha || 0.1 || alpha parameter of the bayesian inference model||
||beta || 0.1 || beta parameter of the bayesian inference model||
||mu || 0.0 || mu parameter of the bayesian inference model||